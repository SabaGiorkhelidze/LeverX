load data from file

combile data into list of room -> each room includes the students assigned to it

export it as json

accept input params via cmd


room id == student room


{
    id: int #room_id
    name: str #room_name
    student: list[stundets] #student_obj
}